---
adr: none
---
### Docs
- Roadmap tracker moved to project mode: its config now lives in `docs/roadmap.config.json` and the built `docs/roadmap.data.json` + `docs/roadmap.html` are committed, so `pr-precheck` Check 8 can verify tracker freshness per branch. Regenerated `docs/_index.md`, which still linked the removed release-skills design spec.

### Perf
- N/A — documentation only
